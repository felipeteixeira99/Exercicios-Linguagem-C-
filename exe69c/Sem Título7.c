decimo <- termo + (10 - 1) * razao

   Para cont <- termo ate decimo passo razao faca:
      escreva(termo," ->")
      soma <- soma + termo
      termo <- termo + razao


   Fimpara
   escreval(" Acabou!")
   escreval("A soma dos termos é:",soma)
